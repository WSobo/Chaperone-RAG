# Dependencies are declared in pyproject.toml (the source of truth).
# Preferred workflow is uv: `uv sync` (see README / CLAUDE.md). This file is a
# plain-pip fallback for tooling that expects requirements.txt.
#
#   pip install -e .            # core: CPU retrieval pipeline + mock LLM (runs anywhere)
#   pip install -e ".[gpu]"     # + torch/transformers/accelerate for the Gemma backend
#   pip install -e ".[eval]"    # + ragas/datasets for the evaluation harness
#   pip install -e ".[dev]"     # + pytest/ruff/mypy
#
# On the HPC cluster, scripts/setup_env.sh installs uv and syncs the GPU env (it also
# compiles google-deepmind/gemma from source until that's pip-installable).
-e .
